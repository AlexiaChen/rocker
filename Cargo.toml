[workspace]
members = [
  "cgroups",
  "container",
  "network",
  "namespace",
  "rocker",
]

[profile.dev]
overflow-checks = true
panic = "unwind"

[profile.release]
incremental = false
lto = true
overflow-checks = false
panic = "unwind"
